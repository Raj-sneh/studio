'use client';
/**
 * REDUNDANT FILE REMOVED
 * The useUser hook is now centrally managed in src/firebase/provider.tsx
 * to prevent module resolution conflicts during server startup.
 * 
 * This file is kept as a stub to prevent import errors in old build caches.
 */
export {};
